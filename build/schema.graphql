type Holder @entity {
  id: ID!
  address: Bytes!
  balance: BigInt!
  token: Token!
  blockNumber: BigInt!
  timestamp: BigInt!
}

type Token @entity {
  id: ID!
  address: Bytes!
  decimals: Int
  totalSupply: BigInt
  name: String
}